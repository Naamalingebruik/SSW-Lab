# Build-tijdelijke bestanden (zip alleen voor release-upload)
build/*.zip

# Gecompileerde executables — worden gedistribueerd via GitHub Releases
executables/*.exe

# Log- en temp-bestanden
*.log
*.tmp

# Word/Excel tijdbestanden
~$*

# ISOs (te groot voor git)
*.iso

# Persoonlijke lokale configuratie (domein, tenant, etc.)
config.local.ps1
